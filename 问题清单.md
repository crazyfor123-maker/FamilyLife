# 家族人生录 - 全量测试问题清单

> 测试时间：2026-08-27 08:00~09:00
> 测试范围：编译构建、后端API、数据库、前后端联调、安全
> 测试方法：curl 接口测试 + 代码审查 + 构建测试

---

## 🔴 严重问题（阻塞功能）

### 1. 前端所有页面（除登录页外）全部使用 MockData，未接入后端 API

**影响范围：** HomePage、FamilyTreePage、InterviewListPage、TimelinePage、ProfilePage、EventsPage、MessageBoardPage、SearchPage、PersonPage、LifeBookPage 全部 10 个页面

**现状：** 所有页面通过 `import { MockData } from '../data/mockData'` 使用假数据，`api/index.js` 中定义了完整的 API 调用方法但从未被调用。

**后果：** 应用无法与后端交互，数据无法持久化，无法真正使用。

**修复建议：** 逐个页面将 MockData 替换为 api/index.js 中的真实调用。

---

### 2. 前端 API 调用路径与后端路由不匹配

| # | 前端调用路径 | 后端实际路径 | 说明 |
|---|------------|------------|------|
| 1 | `/family/create` POST body `{name: "xxx"}` | `/family/create` POST body `{space_name: "xxx"}` | 字段名不一致：`name` vs `space_name` |
| 2 | `/family/create` body `{foundingYear: 2020}` | `{founding_year: 2020}` | 字段命名风格不一致：驼峰 vs 下划线 |
| 3 | `/family/create` body `{origin: "北京"}` | `{origin: "北京"}` | ✅ 一致 |
| 4 | `/search/search?q=朱&space_id=xxx` | `/search/search?q=朱&space_id=xxx` | ✅ 一致 |
| 5 | `/storage/list?space_id=xxx` | `/storage/list/:spaceId` (path param) | ❌ 前端用 query param，后端用 path param |
| 6 | `/storage/sync/incremental/xxx` | `/storage/sync/incremental/:spaceId` | ✅ 一致 |
| 7 | `/collab/create` POST | `/collab/create` POST | ✅ 一致 |
| 8 | `/comments?story_id=s1` | 未注册 `/comments` 路由 | 后端挂载在 `/api/comments`，但 route.js 内无 GET list 路由 |
| 9 | `/interview/outline/default` | 无此路由 | 前端调用此接口但后端不存在 |
| 10 | `/share/{bookId}` POST | `/share/generate` POST | 路径不匹配 |

---

### 3. 前端 family create 接口字段名不匹配

**前端代码（api/index.js）：**
```js
create(data) { return request('/family/create', { method: 'POST', body: data }); }
```

**前端页面（HomePage.jsx）传递：**
```js
{name: '朱氏家族', motto: '...', foundingYear: 1920, origin: '浙江绍兴'}
```

**后端期望：**
```js
{ space_name: '朱氏家族', motto: '...', founding_year: 1920, origin: '浙江绍兴' }
```

**结果：** `space_name` 为 undefined → 校验失败 → 返回 "家族名称需2-20个字符"

---

## 🟡 中等问题（功能缺陷）

### 4. 搜索接口返回 HTML（404）

**前端调用：** `/api/search/search?q=朱&space_id=xxx`
**后端路由：** `/api/search/search` (GET)
**测试结果：** 返回 404 HTML

**根因：** 搜索路由注册为 `router.get('/search', ...)`，挂载后完整路径为 `/api/search/search`。但测试时 token 过期导致返回 HTML。需确认实际运行时是否正常。

---

### 5. 存储接口返回 HTML（404）

**前端调用：** `/api/storage/list?space_id=xxx`
**后端实际：** `/api/storage/list/:spaceId`（path param）
**结果：** 路径不匹配返回 404 HTML

---

### 6. 评论接口无 list 路由

**后端挂载：** `app.use('/api/comments', commentRoutes)`
**routes/comment.js：** 只有 `createComment`、`getComments`、`likeComment`、`deleteComment`、`getReplies`、`replyComment` 函数
**问题：** 这些函数没有被路由注册（没有 `router.get('/...'` 等）
**结果：** 前端调用 `/api/comments?story_id=s1` 返回 404 HTML

---

### 7. 采访大纲接口不存在

**前端调用：** `GET /api/interview/outline/default`
**后端路由：** 无此路径
**后端有：** `/interview/list/:personId`、`/interview/create`、`/interview/:sessionId` 等
**结果：** 返回 404 HTML

---

### 8. 分享链接生成接口路径不匹配

**前端调用：** `POST /api/share/{bookId}`
**后端路由：** `POST /api/share/generate`
**结果：** 404

---

### 9. Person create 返回 "请先加入家族空间"

**后端中间件：** person create 路由的 authenticate 中间件检查用户是否加入了 family_space
**问题：** 新用户登录后不在任何家族中，无法创建人物
**影响：** 正常用户流程断裂（注册→登录→无法创建人物）

---

### 10. TTS 合成失败返回占位音频

**测试结果：** `TTS合成失败，使用占位音频`
**根因：** `synthesize()` 使用系统 `say` 命令，但 macOS 上可能缺少语音或命令执行超时
**影响：** 语音采访功能不可用

---

### 11. OCR 所有引擎都返回模拟结果

- `recognizeWithTesseract` → 返回提示使用前端引擎
- `recognizeWithAli` → "阿里云OCR未配置"
- `recognizeWithBaidu` → "百度OCR未配置"
- `getMockResult` → 返回假数据

**影响：** OCR 功能完全不可用（无配置时）

---

### 12. ASR 所有引擎都返回占位结果

- `recognize` → 返回 `[ASR转写结果 - 需要配置ASR引擎]`
- `recognizeAli` → "阿里云ASR引擎未配置"
- `recognizeIflytek` → "讯飞ASR引擎未配置"

**影响：** 语音转文字功能完全不可用（无配置时）

---

### 13. AI 服务全部使用本地规则引擎

**AI_CONFIG.provider = 'local'**
**generateQuestionLocal：** 基于正则和关键词匹配，无真实 AI 能力
**generateChapter：** 基于模板的占位文本
**影响：** AI 追问、人生之书生成等功能无真实智能

---

## 🟢 低优先级问题（代码质量/健壮性）

### 14. JWT 密钥硬编码

**位置：** `backend/src/routes/auth.js`
```js
process.env.JWT_SECRET || 'family-life-record-secret-2024'
```
**风险：** 无环境变量时默认密钥公开，可伪造任意用户 token

---

### 15. SQL 注入风险

**位置：** `backend/src/routes/member.js` L20-21
```js
const roleFilter = req.query.role ? `AND sm.role = '${req.query.role}'` : '';
const nameFilter = req.query.name ? `AND ua.nickname LIKE '%${req.query.name}%'` : '';
```
**问题：** 直接拼接用户输入到 SQL，无参数化

---

### 16. 验证码无速率限制

**位置：** `backend/src/routes/auth.js` L18
- 每日 10 次限制存在
- 但 IP 维度无限制，可无限注册账号

---

### 17. 数据库密码为空

**位置：** `backend/src/config/db.js`
```js
password: process.env.DB_PASSWORD || ''
```
**风险：** 无密码连接 MySQL，生产环境极度危险

---

### 18. 协同编辑会话内存存储

**位置：** `backend/src/services/collab.js`
```js
this.sessions = new Map(); // 内存存储
```
**问题：** 服务重启后所有协同会话丢失，无持久化

---

### 19. 文件上传无校验

**位置：** `backend/src/services/ocr.js`
- 无文件大小限制校验
- 无文件类型校验
- 无恶意文件检测

---

### 20. 数据库初始化忽略部分错误

**位置：** `backend/src/utils/init_db.js`
```js
const ignored = ['already exists', 'Duplicate key name', 'Duplicate column name', "Unknown column", ...];
```
**问题：** 静默忽略错误，可能掩盖真正的建表失败

---

### 21. 后端 WebSocket 连接无认证

**位置：** `backend/src/app.js` L70-73
```js
ws.on('connection', (client) => {
  // 无 token 验证
});
```
**风险：** 任何人都可连接 WebSocket

---

### 22. 前端构建产物路径硬编码

**位置：** `backend/src/app.js` L170
```js
const distPath = path.join(__dirname, '../../dist');
```
**问题：** 部署路径不一致时无法找到前端文件

---

### 23. 缺少健康检查的数据库状态

**`/api/health` 返回：** `{"status":"ok","ws":"available"}`
**问题：** 不检查数据库连接状态，数据库宕机时仍返回 200 OK

---

### 24. 删除家族空间无级联保护

**位置：** `backend/src/routes/family.js` L126-129
```js
await run('DELETE FROM space_member WHERE space_id = ?', [spaceId]);
await run('DELETE FROM family_space WHERE space_id = ?', [spaceId]);
```
**问题：** 删除家族后，该家族下的人物档案、故事、采访等数据成为孤儿数据

---

### 25. 前端 index.html 中 script 标签未注册所有页面

**需要检查：** `index.html` 是否包含所有新页面的 script 注册（LifeBookGeneratePage、BookReaderPage 等新页面）

---

## 📊 测试汇总

| 测试项 | 结果 | 详情 |
|--------|------|------|
| 前端构建 | ✅ 通过 | vite build 266ms，48 modules |
| 后端语法检查 | ✅ 通过 | 所有 .js 文件 node -c 检查通过 |
| 数据库初始化 | ✅ 通过 | 22 条语句执行成功 |
| 后端服务启动 | ✅ 通过 | HTTP:3000, WebSocket:3000 |
| 健康检查接口 | ✅ 通过 | 返回正常 |
| 验证码发送 | ✅ 通过 | 正常返回 |
| 登录接口 | ✅ 通过 | 正常返回 token |
| 家族创建 | ⚠️ 字段名不匹配 | name vs space_name |
| 家族列表 | ✅ 通过 | 正常返回 |
| 人物创建 | ⚠️ 未加入家族 | 新用户无法创建人物 |
| 时间线列表 | ✅ 通过 | 正常返回空数组 |
| 大事记列表 | ✅ 通过 | 正常返回空数组 |
| 留言板列表 | ✅ 通过 | 正常返回空数组 |
| 留言发布 | ✅ 通过 | 正常返回 |
| TTS 配置 | ✅ 通过 | 返回语音列表 |
| TTS 合成 | ❌ 失败 | 返回占位音频 |
| OCR 配置 | ✅ 通过 | 返回配置 |
| AI 配置 | ✅ 通过 | 返回本地引擎 |
| 搜索接口 | ❌ 404 | 路径不匹配 |
| 存储接口 | ❌ 404 | 路径不匹配 |
| 分享接口 | ❌ 404 | 路径不匹配 |
| 评论接口 | ❌ 404 | 无 list 路由 |
| 采访大纲 | ❌ 404 | 路由不存在 |
| 协同编辑 | ⚠️ 内存存储 | 重启丢失 |
| WebSocket | ⚠️ 无认证 | 任何人都可连接 |
| SQL 注入防护 | ❌ 缺失 | member 路由拼接 SQL |
| JWT 密钥 | ⚠️ 硬编码 | 默认密码公开 |
| 前端 API 接入 | ❌ 未接入 | 10 个页面全部用 MockData |

---

## 📋 修复优先级

### P0 - 立即修复
1. **前端接入后端 API**（10 个页面全部使用 MockData）
2. **修复字段名不匹配**（name→space_name, foundingYear→founding_year）
3. **修复路由路径不匹配**（search/storage/share/comments/interview/outline）
4. **新增评论 list 路由**
5. **新增采访大纲路由**

### P1 - 本周修复
6. **JWT 密钥使用环境变量**
7. **SQL 注入修复**（member 路由）
8. **数据库密码配置**
9. **WebSocket 认证**
10. **新用户自动加入家族**（或取消认证检查）

### P2 - 后续优化
11. TTS/OCR/ASR 配置文档
12. AI 服务配置指南
13. 协同编辑持久化
14. 删除级联保护
15. 健康检查增加数据库检测
16. 文件上传安全校验
